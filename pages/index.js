import Head from 'next/head'

const Home = ({auth}) => {
    return (
        <div>
            <Head>
                <title>Cheating - Sign In</title>
            </Head>
            Welcome to Home
        </div>
    )
};

export default Home;